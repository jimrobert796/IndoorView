<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/bg_dialog_success">

    <!-- Icono de éxito (opcional) -->
    <ImageView
        android:id="@+id/iv_success"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:layout_marginTop="8dp"
        android:src="@drawable/ic_check_circle"
        android:contentDescription="Éxito" />

    <!-- Título -->
    <TextView
        android:id="@+id/tv_titulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:text="¡Contraseña reestablecida!"
        android:textColor="#FFCC00"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Mensaje -->
    <TextView
        android:id="@+id/tv_mensaje"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="12dp"
        android:text="Su contraseña fue reestablecida correctamente. Puede volver al inicio."
        android:textColor="#E0E0E0"
        android:textSize="14sp"
        android:gravity="center" />

    <!-- Botón Vamos -->
    <Button
        android:id="@+id/btn_vamos"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="8dp"
        android:backgroundTint="#2196F3"
        android:text="Vamos"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:textStyle="bold" />

</LinearLayout>