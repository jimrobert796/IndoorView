<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">
    <!--Habilita que entre dentro del cell-->

    <!-- CONTENEDOR PRINCIPAL (donde irá el mapa o los fragments) -->
    <FrameLayout
        android:id="@+id/contenedor_principal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation" />

    <!-- BOTTOM SHEET (se desliza desde abajo al tocar un pin) -->
    <include
        android:id="@+id/bottom_sheet_detalle"
        layout="@layout/bottom_sheet_detalle_lugar"
        android:visibility="gone" />

    <!-- BARRA DE NAVEGACIÓN INFERIOR -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_gravity="bottom"
        android:background="@color/black"
        android:elevation="8dp"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:menu="@menu/bottom_nav_menu" />

    <!-- Layout Height a 80 wil sale con el texto -->

</androidx.coordinatorlayout.widget.CoordinatorLayout>